static readonly paramKeys = {
  {{#each encodes.endPoints}}
  {{#each operations}}
  {{{name}}}: [
    {{#each ../parameters}}
    '{{{name}}}',
    {{/each}}
    {{#each request.queryParameters}}
    '{{{name}}}',
    {{/each}}
  ] as const,
  {{{name}}}Required: [
    {{#each ../parameters}}
    '{{{name}}}',
    {{/each}}
    {{#each request.queryParameters}}
    {{#if (is required "true")}}
    '{{{name}}}',
    {{/if}}
    {{/each}}
  ] as const,
  {{/each}}
  {{/each}}
};
